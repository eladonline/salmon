// --- Collections ---/
export const srvData = state => {
  return state.apiData.results;
};

export const heroData = state => {
  return state.apiData.heroData;
};

export const cardDataById = state => {
  return state.apiData.cardDataById;
};
