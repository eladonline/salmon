import React from 'react';
import { Nav, NavItem } from 'react-bootstrap';

const LoginItems = () => (
  <Nav pullRight>
    <NavItem>im a link</NavItem>
    <NavItem>Login</NavItem>
  </Nav>
);

export default LoginItems;
